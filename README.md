

快速安装
bash <(curl -L https://raw.githubusercontent.com/qingfengpian/neovim_config_git/master/utils/install.sh | sed 's/\r$//') 


# Neovim Config
这是我的 Neovim 配置仓库，包含常用插件和设置。
## 安装方法
在终端执行以下命令即可自动安装：
```sh
sh <(curl -L https://raw.githubusercontent.com/qingfengpian/neovim_config_git/master/utils/install.sh)
---------------
这样别人看到 README 就能直接复制这一行命令运行，效果和你想要的 `sh <(curl ...)` 格式一致。  
要不要我帮你再加一个 **“快速更新配置”** 的命令，比如：
```sh
cd ~/.config/nvim && git pull










